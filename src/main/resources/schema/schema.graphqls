type Query {
    optimizeWaypoints(directions: DirectionsInput!): Directions
}

type Directions {
    origin: String!
    destination: String!
    waypoints: [String!]!
}

input DirectionsInput {
    origin: String!
    destination: String!
    waypoints: [String!]!
}